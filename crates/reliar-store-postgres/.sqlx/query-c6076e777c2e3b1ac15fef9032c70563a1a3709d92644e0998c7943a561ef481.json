{
  "db_name": "PostgreSQL",
  "query": "SELECT\n                       count(*) FILTER (\n                           WHERE published_at IS NULL AND dead_at IS NULL\n                             AND available_at <= now()\n                             AND (locked_until IS NULL OR locked_until < now())\n                             AND (expires_at IS NULL OR expires_at > now())\n                       ) AS \"pending!\",\n                       count(*) FILTER (WHERE dead_at IS NOT NULL) AS \"dead!\",\n                       count(*) FILTER (\n                           WHERE published_at IS NULL AND dead_at IS NULL\n                             AND expires_at IS NOT NULL AND expires_at < now()\n                       ) AS \"expired_pending!\",\n                       min(available_at) FILTER (\n                           WHERE published_at IS NULL AND dead_at IS NULL\n                             AND available_at <= now()\n                             AND (locked_until IS NULL OR locked_until < now())\n                             AND (expires_at IS NULL OR expires_at > now())\n                       ) AS oldest_pending_available_at,\n                       now() AS \"as_of!\"\n                     FROM outbox",
  "describe": {
    "columns": [
      {
        "ordinal": 0,
        "name": "pending!",
        "type_info": "Int8",
        "origin": "Expression"
      },
      {
        "ordinal": 1,
        "name": "dead!",
        "type_info": "Int8",
        "origin": "Expression"
      },
      {
        "ordinal": 2,
        "name": "expired_pending!",
        "type_info": "Int8",
        "origin": "Expression"
      },
      {
        "ordinal": 3,
        "name": "oldest_pending_available_at",
        "type_info": "Timestamptz",
        "origin": "Expression"
      },
      {
        "ordinal": 4,
        "name": "as_of!",
        "type_info": "Timestamptz",
        "origin": "Expression"
      }
    ],
    "parameters": {
      "Left": []
    },
    "nullable": [
      null,
      null,
      null,
      null,
      null
    ]
  },
  "hash": "c6076e777c2e3b1ac15fef9032c70563a1a3709d92644e0998c7943a561ef481"
}
