---
description: Create a markdown task card in the backlog repo (../sisa-reliar-backlog/docs/backlog/)
argument-hint: <task title/description>
---
Create a new task card under `../sisa-reliar-backlog/docs/backlog/` following `team/task-board.md` for:

$ARGUMENTS

Assign the next `RELIAR-<n>` id, fill the frontmatter (type, status: `todo`, owner, crate, priority,
srs sections, labels, estimate, created/updated), and write the `## Context` and the
`## Acceptance criteria` (Given/When/Then — you own these as PO). Print the new file path and a
one-line summary.
